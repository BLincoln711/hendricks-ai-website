# Implementation Plan: redesign-2026-09

Status tracking for the seventeen-PR build order in the redesign developer handoff (`~/claudecode/hendricks-redesign-2026-09/18-developer-handoff.md`, section 11). Branch `redesign-2026-09` from `main` at `f2006e8`; every PR targets the branch, and the branch merges to `main` only when the handoff's section 10 launch checklist is green.

This file replaces the Cursor-era plan, which tracked the original handoff kit in `docs/` and was closed out at Phase 6. Positioning, copy rules and locked strings live in `AGENTS.md` and `_inputs/CANON.md`; this file records only what has shipped.

Gate means what must be green before merge, on top of `pnpm verify` as it then stands. Status is one of: not started, in progress, done (commit), deferred (reason).

| PR | Scope | Gate | Status |
|---|---|---|---|
| 1 | Truth and hygiene, no visual change: the 4.2 observed-systems drift fixes and rewritten boundary test with fixtures under `tests/fixtures/content/`; CM-14 comments; this file replaced; `@lhci/cli` with `lighthouserc.js` informational | Within `src`, `content` and `tests`, "three systems" appears only in dated scope rows, the allowlisted 2026-08-19 sentence and the failing fixture; `pnpm test` green including `analytics.test.ts` | done |
| 2 | Tokens: 5.1 to 5.6, `src/styles/`, `check:tokens` and `check:contrast` registered; `.on-plate` transitional class | `check:contrast` and `check:tokens` green; zero console errors at 1440, 1024, 390, 320 | done (be0b535) |
| 3 | Shell: layout, header, navigation, footer, consent, wordmark; Research restored to primary navigation (R6 default); PNG wordmark placeholder | `keyboard.spec.ts`; `layout.spec.ts` MG-03; `consent.spec.ts`; KF-01 to KF-10 | done |
| 4 | Primitives: remaining phase 2 rows, retokens, 404, error, global-error; `onNavy` and `outlineOnNavy` no-ops. No deletions | axe on every route; SM-01 to SM-08; `names.spec.ts` | not started |
| 5 | Plate system, static: `selection-map-data.ts` with invariant test, phase 3 RSC components, `check:census` registered. No deletions | `plate-render.test.ts`; no-JS render; VZ-01, 02, 05 to 08, 11, 12; `check:census` | not started |
| 6 | Motion and the island: `motion.ts`, `plate-controls.tsx` via `next/dynamic`, ladder selection, toggle, `useAnnounce`, grep gates | `motion.spec.ts` and `instrument.spec.ts`; MO-01 to MO-05; VZ-03, 04, 09, 10; `motion.ts` under 3 KB; chunk under 50 KB | not started |
| 7 | Homepage rebuilt to the eleven sections; `home.ts` and `01-home.md` per 4.1; deletes `visuals/selection-map.tsx`; mirror check made blocking; `check:content` adopts the observed-systems guard (4.7 rule 2) | `homepage.spec.ts`; MG-01 on `/`; gate map green; CTA-label dump | not started |
| 8 | Forms infrastructure: 6.4 modules, action and primitives, `events.ts`, `gtag.ts` allowlist, `trackEvent` buffer, CSV rows; the 5.48 necessary category rendered as a checked and disabled 5.29 control in the preferences dialog (deferred from PR 3, which ships it as text). No page mounts a form | `forms.test.ts`; `analytics.test.ts`; blocker 8 e2e | not started |
| 9 | /diagnostic: ten regions, phase table and plate, six-tile strip, fit tool, application at `#apply` | DX-01 to DX-09, DX-38 to DX-41; FM-01 to FM-12; fit network test; dollar guard; MG-01 | not started |
| 10 | /for-agencies and /contact: the two forms, preselects, model rows, pointers, both mirrors; deletes `visuals/partnership-models.tsx` | Submissions reach the action on all three forms; KF-07 with the sheet open; preselects without JavaScript; query redaction | not started |
| 11 | Commercial pages: /solutions ledger, the four solution pages, /how-it-works, /for-brands, /about, /methodology, /corrections; five visual deletions | `routes.spec.ts`; `link-map.test.ts` (L-01, L-04); `json-ld.test.ts` S-02 | not started |
| 12 | Research contract and migration: 4.5 applied, study and editorial migrations, RT-50 rules, `vocabulary.ts` | Typecheck on removed keys; `research-contract.test.ts`; RT-50 fixtures | not started |
| 13 | Research templates and hub; `case-study-record.tsx`; four deletions after PRs 9, 10 and 11 | RT-01 to RT-64 as applicable; RC-01 to RC-11, RC-17 to RC-19; CS tests; MG-01 on the flagship | not started |
| 14 | SEO and crawler: section 9 in full, `check-jsonld.ts`, `robots.test.ts`, STRATEGY.md 4.7 diff filed | `check:jsonld`; `feed.test.ts`; `seo.spec.ts`; `curl -sI` on the artifacts | not started |
| 15 | Sanity cleanup: `enableSanityVisualEditing` and `/studio` disallow removed; unread `SANITY_*` entries retired | P-02 path count updated | not started |
| 16 | Gates and budgets: `check-size.ts`, `check-census.ts`, Lighthouse CI blocking, Mono subsetting, web-vitals route behind `WEB_VITALS_BEACON=off`, `verify` final order | MG-05; shell under 180 KB on every route or a recorded variance | not started |
| 17 | Docs and records: section 10 supersession notes, this file closed out, CONTENT_VERIFICATION reconciled, manual script appended, memory and vault notes | Review | not started |

## Dependencies

PRs 12 to 14 depend on 4 and run in parallel with 7 to 11, except PR 13's deletions, which wait on 9, 10 and 11. PR 5's geometry and PR 6's controller wait on register decision B3; everything else in them does not. A component is deleted only in the PR that rebuilds its last consumer, so typecheck, build and `pnpm verify` stay green at every PR.

## Decisions the build is waiting on

Recorded in handoff section 3. Group A (R1 hero H1, Diagnostic H1, R4 label overrides, fee state, R5 proof line, fit H2, hero boundary noun, R6 navigation) and group C (content-gate rows) are Brandon's, and a gated line renders its approved fallback until its row is `approved`. Closing CONTENT_VERIFICATION rows R5 and R6 and adding L9 are register acts, not commits.

## PR 1 record

- Observed-systems drift (audit V7, CM-02, CM-03, CM-06, SEO-14): `selection-intelligence.ts`, `search-impact-measurement.ts` and `no-shared-source-across-engines.ts` render the shared sentence or the run-date boundary; eleven `content/pages` mirrors re-mirrored to their `.ts` twins; the four dated studies gain a change-history table with a `scope` row; `24-corrections.md` gains the three 2026-09-01 entries from `corrections.ts`.
- Guard: `scripts/lib/observed-systems-guard.ts` is the one rule; `tests/unit/observed-systems-boundary.test.ts` runs it over `src/content`, `content/pages` and the fixtures. `check:content` will adopt it under handoff 4.7 rule 2 in PR 7, beside the rule 3 mirror check; until then the unit test is its only caller. Coverage claims are checked per sentence, the scope-row exception requires a dated row, and a retyped copy of `observedSystemsContext` is flagged the same way a retyped count is.
- `tests/setup.ts` restores jsdom's `localStorage` over Node 25's inert Web Storage global, which was why the five GA4 send-gate tests failed on `main`.
- `docs/17`, `docs/19` and `docs/20` still carry three-system lines; they take PR 17's supersession notes.

## PR 3 record

- Shell (09 5.1 to 5.6, 5.54, 5.58, 5.59, 5.60): `site-shell.tsx` owns the whole chrome in KF-02 order (skip link, header, `main`, live region, footer, consent sheet) and provides the shared announcer; `ConsentManager` moved into it from the root layout. New `layout/live-region.tsx` (`AnnouncerProvider`, `LiveRegion`, `useAnnounce`; debounce from `src/lib/motion.ts`), `layout/grid.tsx`, and `layout/nav-link.tsx`, the one client leaf that reads `usePathname` for `aria-current` (KF-10) and the header button's `#fit` override on /diagnostic (DX-05, register B1); the footer itself stays a server component, which is why the 11 section 8 client list gains this file.
- Research restored to primary navigation between For Agencies and About (CANON R6 default); `isCurrentRoute` and `headerCtaHref` live in `src/config/navigation.ts` with unit tests; the panel lists the four solution names, so the navigation `description` field went with its only consumer.
- Wordmark: the retokened PNG placeholder (handoff 5.8) at `--header-wordmark-height`, no `priority`, no `sizes`; header link named "Hendricks, home", footer image named "Hendricks"; `tone` removed. The dark PNG has no consumer and goes with the SVG commit, as 5.8 schedules.
- Consent sheet at 16 decision 1's default: light, three secondary 44 px buttons stacked below 640 px and in one equal row above, `min(360px, 40vh)` cap below 1024 px with only the body scrolling, a single-row bar from 1024 px, the approved four-sentence body with the Privacy Notice link inline; after a decision focus moves to `main` and the saved-choice line goes through the announcer. Preferences dialog retokened; because it is controlled with no `Dialog.Trigger`, the close handler restores focus itself (opener, else `main`).
- `globals.css` gains an `@layer components` block carrying the four `@starting-style` mount-time entries (11 section 8 item 6): Solutions panel, mobile sheet and overlay, consent sheet, preferences dialog.
- Token file correction: the 22.4375rem step in `tokens.css` section 5 now follows the 44.9375rem step; in `09-tokens.css` source order both matched at 320 px and the later block shadowed the 20 px wordmark and 8 px header gap. The deliverable still carries the shadowed order.
- Chrome string introduced from the hi-fi and not yet in the 02 section 6 register: "Show the four solutions" (the Solutions disclosure button's accessible name, header and sheet). Needs its row before the 4.7 rule 10 chrome check lands.
- Tests: `tests/e2e/keyboard.spec.ts` (KF-01 to KF-10 over the shell), `tests/e2e/layout.spec.ts` (MG-03 on every built route at 320 by 568, 320 by 800 and 390 by 844; the two-sentence body variant waits on counsel), `consent.spec.ts` extended (three-way equal prominence, pre-dismissal visibility, no focus steal, focus to `main`, announcer, Privacy Choices semantics), `tests/unit/live-region.test.tsx`, navigation cases in `content.test.ts`.
- Review fixes: `/diagnostic` carries the `#fit` anchor now (`id` from `DIAGNOSTIC_FIT_ID`, `tabIndex={-1}`; the KF-07 landing offset comes from the `html` `scroll-padding-top` every fragment inherits, so DX-25's per-anchor `scroll-margin-top` is not duplicated, which would double the offset; the header button and the sheet button render a plain anchor when the destination is a fragment, since next/link keeps focus on the link for a same-page hash while native fragment navigation moves it to the target); Escape from inside the Solutions panel returns focus to the chevron before the panel hides; the footer legal row is a `ul` with the Privacy Choices button placed by `PRIVACY_CHOICES_POSITION`, not beside a named route; the sheet's disclosure state resets on close; the KF-04 inventory moved to `tests/unit/modal-inventory.test.ts`.
- Package corrections to file in PR 17 (the design package is read-only input): 09 5.3 header button destination on /diagnostic is `#apply` while 14 DX-05 and decision 3 say `#fit`, the build follows DX-05; 09-tokens.css section 5 block order (above); a 02 section 6 register row for "Show the four solutions" (above); `layout/nav-link.tsx` added to the 11 section 8 closed client-boundary list and the handoff section 6 preamble; 14 DX-25's `scroll-margin-top` noted as already supplied by `scroll-padding-top` in `globals.css`.
- For Brandon under 16 decision 1: at 320 by 568 the `min(360px, 40vh)` cap resolves to 227 px, which the pinned title and three stacked 44 px buttons nearly fill, so the approved four-sentence body is effectively unreadable at that size. Either the two-sentence body lands sooner (counsel) or the cap loosens below 600 px (for example `min(360px, 48vh)`), with KF-07 re-run.

