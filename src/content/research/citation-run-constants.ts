/**
 * Locked citation-presence measurementTechnique.
 *
 * Kept in a file with no filesystem imports so JSON-LD builders can read it
 * without pulling Node APIs into the schema module.
 */
export const CITATION_PROBE_MEASUREMENT_TECHNIQUE =
  'First-party probe: one question per engine per cell; cited URL hosts recorded per cell'
